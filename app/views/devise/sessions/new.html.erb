<div class="login-page-container">
  <h1 class="title">diip</h1>

  <h2 class="description">
    Crie seu catálogo de <p class="highlight">recomendações <span id="highlighted-text-icon">📚</span></p>
  </h2>
  <p class="login-text">Faça seu login</p>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="login-buttons-row">
    
      <%- if devise_mapping.omniauthable? %>
        <%- resource_class.omniauth_providers.each do |provider| %>
          <%- if provider.to_s == "google_oauth2" %>
            <%= link_to(omniauth_authorize_path(resource_name, provider), method: :post) do %>
              <div id="google-btn">
                <%= show_svg('google-icon') %>
              </div>
            <% end %>
          <% end %>

          <%- if provider.to_s == "facebook" %>
            <%= link_to(omniauth_authorize_path(resource_name, provider), method: :post) do %>
              <div id="facebook-btn">
                <%= show_svg('facebook-icon') %>
              </div>            
            <% end %>
          <% end %>          
        <% end %>
      <% end %>

    </div>
  <% end %>

  <div class="spacer">
    <%= render 'layouts/shared/flash_message' %>
  </div>

  <a href="mailto:alan@diip.app" id="privacy-btn">Suporte</a>

</div>